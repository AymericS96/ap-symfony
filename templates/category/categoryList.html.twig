{% extends 'base.html.twig' %}

{% block title %}Hello CategoryController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="container">
   
<table class="table" >
<tr>
    <th>Nom</th>
    <th>Description</th>
    <th>Catégorie</th>
    <th>Editer</th>
    <th>Supprimer</th>
</tr>
{% for category in listeCategory %}
<tr>
    <td>{{category.name}}</td>
    <td>{{category.description}}</td>
    <td>{{category.slug}}</td>
    <td>
        <a href="{{path('admin_editCategory',{'id':category.id})}}"> 
            Editer 
        </a>
    </td>
    <td><a href="{{path('admin_deleteCategory',{'id':category.id})}}"> 
            Supprimer 
        </a>
    </td>

</tr>

{% endfor %}
</table>

</div>
{% endblock %}
